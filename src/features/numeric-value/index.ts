import { NumericValueProperties } from "./properties";
import { NumericValueNode } from "./node";
import { NumericValueService } from "./service";

export const NumericValue = {
  component: NumericValueNode,
  service: NumericValueService,
  properties: NumericValueProperties,
};
